<UserControl x:Class="JournalApp.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JournalApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock 
            Grid.Row ="0"
            Margin="10, 5"
            Text="History View"
            HorizontalAlignment="Center"
        />
        <ItemsControl 
            x:Name="HistoryItems"
            Grid.Row="1"
        >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Expander Margin="2">
                        <Expander.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Date}" Padding="3"/>
                                <TextBlock Grid.Column="1" Text="{Binding Title}" Padding="3"/>
                                <TextBlock Grid.Column="2" Text="{Binding EType}" Padding="3"/>
                            </Grid>
                        </Expander.Header>
                        <TextBlock Text="{Binding Notes}" Margin="5,2"/>
                    </Expander>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            
        </ItemsControl>
    </Grid>
</UserControl>
