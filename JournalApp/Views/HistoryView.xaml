<UserControl x:Class="JournalApp.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JournalApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock 
            Grid.Row ="0"
            Margin="10, 5"
            Text="History View"
            HorizontalAlignment="Center"
        />
        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="1">
            <ItemsControl 
                x:Name="HistoryItems"
            >
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Margin="2">
                            <Expander.Header>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Date}" TextWrapping="Wrap" Padding="3"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name, StringFormat='- {0}'}" TextWrapping="Wrap" Padding="3"/>
                                    <TextBlock Grid.Column="2" Text="{Binding EType,StringFormat='- {0} -'}" TextWrapping="Wrap" Padding="3"/>
                                    <Button Grid.Column="3" Tag="{Binding}" Click="Delete_Click" Margin="2" Padding="2">Delete Entry</Button>
                                </Grid>
                            </Expander.Header>
                            <Grid>
                                <TextBlock Grid.Column="1" Text="{Binding Notes}" TextWrapping="Wrap" Margin="5,2"/>
                            </Grid>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
